{
  "name": "morning_code_challenge",
  "version": "1.0.0",
  "description": "Given a string of words find the highest scoring word, if two words score the same then return the word that appears earliest in the original string. All letters will be lowercase and all inputs are valid.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "jest --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^22.4.0"
  }
}
